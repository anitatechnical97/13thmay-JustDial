 window.goog_snippet_vars = function(url) {
   var w = window;
   w.google_conversion_id = 1016105228;
   //w.google_conversion_label = url;
   w.google_conversion_label = "qrsDCIqfgmgQjJLC5AM";
   w.google_remarketing_only = false;
 }

window.goog_report_conversion = function(url) {
   goog_snippet_vars(url);
   window.google_conversion_format = "3";
   var opt = new Object();
   opt.onload_callback = function() {
   if (typeof(url) != 'undefined') {
     // window.location = url;
   }
 }

 var conv_handler = window['google_trackConversion'];
 if (typeof(conv_handler) == 'function') {
   conv_handler(opt);
 }
}
